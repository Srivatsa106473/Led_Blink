# Led_Blink
In this example, we connect an LED to an Arduino digital pin. Using pinMode(pin, OUTPUT), we set the pin as output. With digitalWrite(pin, HIGH/LOW), we control LED ON/OFF (5V = HIGH, 0V = LOW). The delay(ms) function pauses execution for a set time, e.g., delay(2000) = 2 seconds.
